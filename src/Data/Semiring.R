(function() {
  list(
    intAdd = function(a) function(b) a + b,
    numAdd = function(a) function(b) a + b,
    intMul = function(a) function(b) a * b,
    numMul = function(a) function(b) a * b
  )
})()
